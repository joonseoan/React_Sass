import React, { Fragment } from 'react';

import { Header, Section } from './Common';

export default () => {
    return (
        <Fragment>
            <Header />
            <Section />
        </Fragment>
    );
}